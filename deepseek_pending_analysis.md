# 外汇量化交易系统挂单功能方案分析报告

**角色**：资深外汇量化系统架构师  
**对象**：MT4 黄金自动化交易系统（Go 服务端 + MQL4 EA）  
**目标**：引入挂单机制，解决市价单在不理想价位成交的问题。  
**当前状态**：EA 执行 `ExecuteSignal` 时忽略 `entry` 字段，直接以市价开仓，当市场波动大时产生严重滑点。

---

## 一、三种方案多维度对比

### 方案 A：EA 端自动判断（服务端不变）
**核心逻辑**  
在 `ExecuteSignal` 内插入一个决策层：  
1. 从 JSON 读取 `entry` 价格。  
2. 计算 `|currentPrice - entry| / ATR`。  
3. 若比值 > 0.5（可配置） → 使用挂单（LIMIT 或 STOP，依赖价格相对位置自动推断）。  
4. 否则沿用现行市价单。

**优点**  
- 无需修改 Go 服务端，改动范围最小，部署风险极低。  
- 决策完全本地化，无网络延迟或状态同步问题。  
- 逻辑封闭在单个函数中，测试、调试容易。  
- 可快速迭代 ATR 阈值或挂单类型选取规则。

**缺点**  
- EA 必须自行计算或从服务端获取 ATR（当前 SIGNAL 不含 ATR，需增加字段或 EA 内调用 iATR）。  
- 挂单类型推断（`BUY_LIMIT` vs `BUY_STOP`）依赖简单规则，可能误解策略原本意图（回调入场 vs 突破入场）。  
- EA 负责挂单生命周期管理，若后续服务端改变观点，EA 无法自动取消旧挂单，可能造成风险敞口。  
- 无法实现服务端统一的超时/取消策略，需要 EA 本地定时巡检。

**复杂度**：★☆☆☆☆（仅增几十行代码）  
**安全性**：★★★★☆（隔离良好，但缺乏中心监管）

### 方案 B：服务端下发新命令类型（PENDING_SIGNAL / CANCEL_PENDING）
**核心逻辑**  
1. Go 策略引擎在生成信号时，评估是否需要挂单，并确定挂单类型、有效期。  
2. 新增命令：`PENDING_SIGNAL`（包含 `pending_type`、`expiration` 等）。  
3. EA 新增 `ExecutePending` 处理，并在成交/超时后通过反馈通道告知服务端。  
4. 服务端可主动下发 `CANCEL_PENDING`。

**优点**  
- 服务端拥有全局策略信息（多时间框架、波动率、新闻事件），能做出更精准的挂单决策。  
- 集中式状态管理，可统一追踪挂单、自动同步账户状态，降低僵尸单风险。  
- EA 逻辑简单：只负责执行命令，不参与决策。  
- 便于后续扩展（如部分平仓、移动止损等）。

**缺点**  
- 需要改动 Go 服务端、API 协议、EA 命令路由，研发成本高。  
- 引入双向通信与状态同步（需 EA 上报成交结果），若采用 HTTP 轮询，延迟和丢失问题突出；若用 WebSocket，架构复杂度剧增。  
- 服务端必须实现挂单状态机（等待成交、超时取消、异常恢复），存储可靠性和重启恢复是难点。  
- 若 EA 与服务器通信中断，挂单可能变成“孤儿”，必须设计本地安全兜底。

**复杂度**：★★★★☆（跨进程状态管理 + 协议扩展）  
**安全性**：★★★★★（理想状态下，中心管控能规避绝大部分风险）

### 方案 C：双单并行（市价单 + 挂单同时发送）
**核心逻辑**  
同一信号同时驱动一个市价单和一个挂单，EA 维护“候选池”，任一成交则立即取消另一个。

**优点**  
- 理论上可最大化成交概率，降低滑点，无论价格是瞬间突破还是缓慢回调都能捕捉。

**缺点**  
- **致命缺陷：双重成交风险**。MT4 的 `OrderSend` 与 `OrderDelete` 并非原子操作。在快速行情中，市价单成交的同时，取消请求可能因网络延迟/服务器队列未处理，导致挂单也触发成交，造成双倍仓位。
- 需要复杂的状态机管理候选单，且必须处理部分成交、保证金不足、经纪商拒绝等边界情况。
- 占用双倍保证金，降低资金利用率。  
- 许多经纪商禁止此类策略（可能被视为滥用执行机制），存在合规风险。

**复杂度**：★★★★★（并发控制 + 异常处理）  
**安全性**：★☆☆☆☆（高风险，极易引发严重事故）

**综合排序**  
- **实现复杂度（低→高）**：A → B → C  
- **安全性（高→低）**：B（理想） → A（可控） → C（危险）

---

## 二、挂单未成交的常见陷阱

1. **跳空滑点**  
   黄金在重大数据/事件驱动下常出现跳空，价格直接跃过挂单指定价位，导致挂单无法触发（LIMIT）或触发在更差价位（STOP 转化为市价单时滑点严重）。

2. **经纪商挂单距离限制**  
   大部分经纪商规定挂单必须距当前市价至少 N 点（例如黄金 200~500 点）。若 entry 过于接近现价，`OrderSend` 将返回 `ERR_INVALID_PRICE`。过远的挂单也可能被拒绝。

3. **Expiration 机制不可完全依赖**  
   - 并非所有经纪商都严格遵守 MT4 的 `expiration` 参数。  
   - 到期检查可能仅在交易时段触发，周末或休市期间可能不会自动删除，导致周一开盘跳空触发。  
   - 建议 EA 本地定时器辅助取消。

4. **资金冻结与拒绝**  
   挂单会预扣保证金（Stop 单可能比 Limit 单占用更多），账户净值不足时将导致挂单失败。

5. **类型匹配错误**  
   若策略本意为回调买入，但 EA 误用 `BUY_STOP`，则价格回落时不会成交，反而在向上突破时成交——完全与策略相悖。因此准确传递挂单类型至关重要。

6. **重复挂单与信号冲突**  
   若同一品种已存在挂单，新信号又下发同向或反向指令，EA 必须定义清晰的合并/取消逻辑。否则可能堆积多张挂单，造成意外成交或保证金不足。

7. **时间风险**  
   超时未成交的挂单若没有自动清理，可能在未来某一时刻被突发波动扫中，而此时策略可能早已改变观点，造成低质量开仓。

---

## 三、LLM 生成 entry 偏离指标位的风险

LLM 提供的 entry 价格若与经典技术指标位（如支撑阻力、布林带、斐波那契回调）偏差较大：

- **挂单可能长期无法成交**（entry 太保守，远离市价），错过最佳入场时机。
- **入场后盈亏比恶化**（entry 太激进，没有给足缓冲），止损更容易被扫。
- **挂单成交概率分布不均**：如果 entry 位于密集成交区之外，可能触发流动性稀薄区域的“扫单”，随后价格迅速回归，造成亏损。

**关键**：entry 的准确性比是否使用挂单更重要。**挂单机制本身不会让交易变差**，它只是“以价格为条件”开仓。如果 entry 质量差，市价单同样会在不良价位入场，而挂单至少提供了不做错的机会。  
**缓解措施**：策略引擎应结合 H1/H4 指标计算 entry，而非仅靠 LLM 输出；EA 端可增加过滤，例如要求 entry 位于合理波动区间（如前 N 根 K 线高低点范围），否则发送警告而不开仓。

---

## 四、MT4 expiration 机制可靠性评估

MT4 `OrderSend` 的 `expiration` 参数是服务端执行的挂单截止时间，其可靠性取决于经纪商实现：

- **多数合规经纪商支持**，在到期后自动删除挂单。
- **局限性**：
  - 部分白标或小型经纪商可能忽略该字段，挂单永久有效。
  - 到期时间基于交易服务器本地时间（通常 GMT），需注意时区转换。
  - 若经纪商服务器重启或存在时间窗口错乱，expiration 可能被延迟。
  - 休市期间可能不触发到期检查，周一开盘挂单若未到期可能直接成交。

**最佳实践**：**双保险策略**  
1. 在挂单时设置 `expiration`（例如 `TimeCurrent() + 30*60`）。  
2. EA 内使用 `OnTimer` 每 10 秒检查一次挂单，手动 `OrderDelete` 超时单。

---

## 五、推荐第一步最小可行方案

**选择方案 A 的增强版**，理由：
- 零成本改动服务端，降低系统耦合度，立即缓解市价单滑点问题。
- 可通过配置阈值和挂单类型推断逻辑，逐步积累经验，为后续服务端改造提供数据。
- 风险受限于 EA 单点，影响范围可控，回滚方便。

### 实施细节

**1. 信号字段扩展（服务端配合一次小幅更新）**  
在 SIGNAL JSON 中增加两个可选字段：
- `atr`：当前周期的 ATR 值（float，例如 14 周期 H1 的 ATR）。  
- `pending_type`：挂单类型建议（`"LIMIT"` 或 `"STOP"`，若为空则 EA 自行推断）。  
这样即使 EA 逻辑完善后，也能平滑过渡到服务端指导。

若服务端暂时不改，EA 内调用 `iATR(NULL, PERIOD_H1, 14, 1)` 自行获取。

**2. EA 核心决策伪代码**
```mql4
void ExecuteSignal(...){
   double entry = ...;
   double atr = GetATR(); // 优先从 cmd 读取，否则计算
   double currentPrice = (type == BUY) ? Ask : Bid;
   double threshold = 0.5 * atr;
   
   if (MathAbs(currentPrice - entry) / atr > 0.5) {
      // 挂单分支
      int pendingCmd = DeterminePendingType(type, entry, currentPrice);
      datetime expiration = TimeCurrent() + 30 * 60; // 30分钟有效
      int ticket = OrderSend(symbol, pendingCmd, lots, entry, slippage, 0, 0, comment, magic, expiration, clrBlue);
      if (ticket < 0) HandleError();
      // 存入挂单监控列表
   } else {
      // 市价分支（保持原逻辑）
   }
}

int DeterminePendingType(string type, double entry, double price) {
   if (type == "BUY")   return (entry < price) ? OP_BUYLIMIT : OP_BUYSTOP;
   if (type == "SELL")  return (entry > price) ? OP_SELLLIMIT : OP_SELLSTOP;
   return -1;
}
```

**3. 挂单生命周期管理**
- `OnTimer()` 每 15 秒遍历挂单，检查是否超时（`OrderOpenTime() + 30*60`）或价格已偏离太