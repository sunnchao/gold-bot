import { describe, expect, it } from 'vitest';
import { TriggerModule } from './trigger.module.js';

describe('TriggerModule', () => {
  it('is defined', () => {
    expect(TriggerModule).toBeDefined();
  });
});
